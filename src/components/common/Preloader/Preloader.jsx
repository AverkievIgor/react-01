import React from "react";
import preloader from "../../../img/loader.gif";

let Preloader = (props) => {
    return  <img src={preloader} width={'300 px'} height={'auto'}/>
}
export default Preloader